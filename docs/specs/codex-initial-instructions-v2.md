# Codex依頼文（改訂版 v2）
# Prompt Vault MVP

> **改訂のポイント**（元仕様からの変更点）
> 1. Docker：将来のSupabase CLI移行前提として目的を明確化
> 2. Playwright E2E：メール/パスワード認証を追加してE2Eを確実に通す
> 3. RLS：「技術的負債」として明示し、将来の対応手順をREADMEに記載する

---

## 目的

Next.js（App Router）で「自分専用 Prompt Vault」を作る。AIに投げる定型プロンプトを保存・再利用できる。コスト最小・簡単優先でMVPを完成させる。将来、認証をBetter Authに移行できる余地を残す。

---

## スタック（確定）

- Next.js（App Router）
- TypeScript（フロント＋サーバー）
- Tailwind CSS
- UI：shadcn/ui（必要最小限でOK）
- バリデーション：Zod
- 認証：Supabase Auth（GitHub OAuth ＋ **メール/パスワード認証を追加**）
- DB：Supabase Postgres
- ORM：Prisma
- Lint/Format：Biome（ESLintは使わない）
- E2E：Playwright（最小1本）
- ホスティング：Vercel（環境変数で動く）
- secrets対策：gitleaks（pre-commit + GitHub Actions）
- Docker：将来のローカルSupabase CLI環境を見据えた構成

---

## 非機能要件（重要）

- 秘密情報をコミットしない仕組み（gitleaks）を入れる
- `.env`系は必ずgitignore（`.env.local`など）
- RLSは今回は使わない。ただし**技術的負債として明示**し、READMEに将来の対応手順を記載する
- アプリ側で必ず所有者チェックをする（RLSの代替として必須）
- 将来のBetter Auth移行を簡単にするため、認証プロバイダのユーザーIDを直接アプリデータのFKにしない（`app_user`を持つ）

---

## 機能要件（MVP）

### 1) 認証・ユーザー紐づけ

- Supabase AuthでGitHubログインできる
- **テスト・開発用にメール/パスワード認証も有効にする**（Supabase Dashboardで`Email Provider`を有効化）
- ログイン済みでないとアプリ機能ページへ入れない（未ログインは`/login`へ）
- ログイン後、Supabase userを取得して`app_user`をupsertする
  - `app_user.authProvider = "supabase"`
  - `app_user.authSubject = supabase user.id`（unique）
  - emailは取れたら保存（optional）
- 今回は自分専用だが、ownerチェックは必ず実装する
- サーバー側でセッションを扱う（Supabase SSR helpers使用）
- **自分専用の担保：allowlistによるアクセス制限**
  - 環境変数 `ALLOW_EMAILS`（カンマ区切り）を用意する
  - ログイン後、emailがallowlistに含まれない場合は即サインアウトしてアクセス拒否する
  - E2Eテストユーザーのemailも `ALLOW_EMAILS` に含める
  - 本番環境（Vercel等）では未設定の場合はエラーで起動を止める（fail fast）。開発環境では未設定時に警告ログを出しつつアクセス拒否でも可

### 2) Prompt CRUD

- Promptの作成・一覧・更新・削除
- 一覧で検索（タイトル/タグ）
- Promptは必ず`ownerId`（`app_user.id`）で絞る
- Zodで入力を検証
  - `title`：必須、最大100文字
  - `body`：必須、最大10,000文字
  - `tags`：配列、最大10個、1タグ最大30文字、トリム

### 3) プレースホルダ（テンプレ機能）

- 本文に含まれるプレースホルダを抽出して入力フォームを自動生成する
- プレースホルダ仕様：`{{UPPER_SNAKE_CASE}}`（例：`{{JOB_DESC}}`、`{{LOGS}}`）
- 入力値を当てて本文をレンダリング（置換）し、完成形テキストを表示
- クリップボードにコピーできる

### 4) テスト（Playwright E2E）

**認証方針：メール/パスワード認証でE2Eを通す**

OAuthはUIの自動操作が不安定なため、E2Eテストには使わない。代わりにSupabaseのメール/パスワード認証でテストユーザーを作成し、安定したE2Eを実現する。

```
テストシナリオ（最小1本）：
1. テストユーザーでメール/パスワードログイン
2. Prompt作成（タイトル・本文・タグ入力）
3. 一覧でPromptが表示されることを確認
4. 詳細でプレースホルダ入力フォームが表示されることを確認
5. 入力値を入れてレンダリング結果を確認
6. クリップボードコピーが機能することを確認（可能な範囲で）
```

**テスト用環境変数（`.env.test.local`）：**
```
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=（テスト用パスワード）
```

> **補足：GitHub OAuthのE2Eについて**
> GitHubのOAuthは外部UIを伴うため自動化が困難。将来的に対応する場合は
> Playwright の `storageState` を使ってセッションを事前に取得する方法が現実的。
> 今回はメール認証E2Eのみ実装し、OAuthは手動確認とする。

---

## データモデル（Prisma）

```prisma
model AppUser {
  id           String   @id @default(uuid())
  authProvider String
  authSubject  String   @unique
  email        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  prompts      Prompt[]
}

model Prompt {
  id        String   @id @default(uuid())
  ownerId   String
  owner     AppUser  @relation(fields: [ownerId], references: [id], onDelete: Cascade)
  title     String
  body      String
  tags      String[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
```

---

## エラーハンドリング方針

### Server Actionsの戻り値は`{ data, error }`形式で統一する

すべてのServer Actionsは以下の型で返す。throwしない。

```ts
type ActionResult<T> =
  | { data: T; error: null }
  | { data: null; error: { code: string; message: string } }
```

呼び出し側（クライアント）は必ず`error`の有無をチェックしてからUIに反映する。

### エラーの表示方法：インライン表示で統一する

トースト通知は使わない。すべてのエラーはフォームのインライン表示（入力欄の下に赤文字）で統一する。

| エラーの種類 | 表示場所 | 例 |
|------------|---------|-----|
| バリデーションエラー | 該当フィールドの直下に赤文字 | 「タイトルは必須です」「100文字以内で入力してください」 |
| サーバーエラー・権限エラー | フォーム上部に赤いエラーバナー | 「保存に失敗しました。再度お試しください。」 |

shadcn/uiの`Sonner`は導入しない。

### ローディング状態

Server Action実行中はボタンを`disabled`にしてスピナーを表示する。
`useTransition`を使ってisPendingを取得し制御する。

---

## UI仕様

### 基本レイアウト：2カラム固定（PCブラウザ専用）

```
┌─────────────────────────────────────────────────────┐
│  Header（ロゴ + ログアウトボタン）                      │
├──────────────┬──────────────────────────────────────┤
│   左ペイン   │           右ペイン                     │
│   （280px）  │          （残り全幅）                   │
│              │                                      │
│ [+ 新規作成] │  PromptタイトルとBody（プレビュー）      │
│              │                                      │
│ [🔍 検索   ] │  ── プレースホルダ入力フォーム ──        │
│              │  {{JOB_DESC}} [..............]        │
│ ─────────── │  {{LOGS}}     [..............]        │
│ タイトルA    │                                      │
│ #tagX #tagY  │  ── レンダリング結果 ──               │
│ ─────────── │  （完成形テキスト表示）                  │
│ タイトルB    │                              [コピー]  │
│ #tagZ        │                                      │
│ ─────────── │  [編集] [削除]                        │
│  ...         │                                      │
└──────────────┴──────────────────────────────────────┘
```

### 各エリアの仕様

**左ペイン（サイドバー）**
- 幅：280px固定、スクロール可能
- 上端に「＋ 新規作成」ボタン（常に表示）
- 検索バー（タイトル/タグでフィルタ、**全件をクライアントに持ちフロント側でフィルタ。サーバーへの問い合わせは行わない。MVPは最大500件程度を想定。超える場合は将来サーバー検索へ切り替える**）
- プロンプト一覧：1アイテムあたりタイトル＋タグを2行以内に収める
- 選択中アイテムをハイライト（背景色変化）

**右ペイン（メインコンテンツ）**
- 未選択時：「プロンプトを選択してください」のプレースホルダ表示
- 選択時：以下を上から順に表示
  1. タイトル（大きめのテキスト）＋タグ一覧
  2. Body本文（読み取り専用プレビュー、スクロール可）
  3. プレースホルダが存在する場合のみ：入力フォームを自動生成
  4. レンダリング結果（コピーボタン付き）
  5. 編集・削除ボタン
- 編集モード：右ペインをそのままフォームに切り替え（別ページに遷移しない）
- **新規作成**：「＋ 新規作成」ボタンを押すと右ペインに空のフォームをそのまま表示（モーダル・別ページへの遷移なし）。左ペインの選択状態はなし。保存完了後に作成したプロンプトを自動選択して詳細表示に切り替える
- **削除**：削除ボタンを押したらshadcn/uiの`AlertDialog`で確認ダイアログを表示する。「キャンセル」「削除する」の2択。確認後に削除実行、完了したら左ペインの先頭アイテムを自動選択する（一覧が空になった場合は未選択状態に戻す）

### shadcn/uiコンポーネント（使用するもの）

- `Button`：新規作成・編集・削除・コピー
- `Input`：検索バー・プレースホルダ入力・フォーム各フィールド・タグ入力
- `Textarea`：Body入力
- `Badge`：タグ表示
- `Separator`：左ペインのアイテム区切り
- `ScrollArea`：左ペイン・右ペイン本文のスクロール制御
- `AlertDialog`：削除確認ダイアログ

### タグ入力UI

カンマ区切りのテキスト入力で統一する（バッジ型は使わない）。

```
タグ（カンマ区切り）: [design, prompt, gpt               ]
                      ↓ 保存時にトリムして配列に変換
                      ["design", "prompt", "gpt"]
```

- 入力欄のplaceholderに `例: design, prompt, gpt` と表示する
- 保存時にカンマで分割→各要素をtrim→空文字を除外してZodで検証
- 表示時（詳細・一覧）はshadcn/uiの`Badge`で1タグずつ表示

### テーマ

- shadcn/uiのデフォルトテーマ（neutral系グレー）をそのまま使用
- ダークモード：shadcn/uiの`ThemeProvider`で対応（トグルボタンをHeaderに設置）
- レスポンシブ対応は**不要**（PCブラウザ専用）

---

## 実装方針（構造）

CRUD方式はServer Actionsを使う。ただしバックエンドっぽく分離する。

```
src/
├── server/
│   ├── actions/       # Server Actionsの入口（薄いラッパー）
│   ├── services/      # Zod検証・ownerチェック等のビジネスロジック
│   └── repositories/  # Prisma操作のみ（DBアクセス層）
├── lib/
│   └── supabase/      # server/browserクライアント（SSR helpers）
├── schemas/           # Zodスキーマ定義
├── utils/
│   └── placeholder.ts # プレースホルダ抽出＆レンダリング
└── app/
    ├── (auth)/
    │   └── login/     # ログインページ
    └── app/
        └── prompts/   # 一覧/作成/詳細
```

**各レイヤーの責務を明確にすること：**
- `actions/`：認証チェック → serviceを呼ぶだけ
- `services/`：Zodバリデーション → ownerチェック → repositoryを呼ぶ
- `repositories/`：Prismaのquery/mutationのみ。ロジックを持たない

---

## Docker（目的を明確化）

**今回のDockerの位置づけ：**
将来、Supabase CLIでローカルDBを立てる際にアプリもコンテナで動かせるよう、Dockerfile/docker-compose.ymlを今のうちに用意しておく。**MVPの普段の開発は`pnpm dev`で行う。**

```yaml
# docker-compose.yml（概要）
services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    # 将来追加予定：
    # supabase:
    #   image: supabase/supabase-local  （Supabase CLI対応時に追加）
```

> **将来の拡張手順（READMEに記載）：**
> Supabase CLIでローカルDB環境を作る場合は `supabase start` を実行し、
> `docker-compose.yml` に supabaseサービスを追加する。

---

## RLS：技術的負債の明示

> **⚠️ 技術的負債（READMEのTODOセクションに記載すること）**
>
> 現在、Supabase PostgresのRow Level Security（RLS）は無効。
> アプリ側のownerチェック（`services/`層）で代替している。
>
> **重要：PrismaによるDB直接接続とRLSの非互換について**
>
> PrismaによるDB直接接続では、リクエストごとのユーザーJWT（`auth.uid()`）が
> DBに伝播しないため、**「ユーザー単位のRLSポリシー」を期待通りに適用するのが
> 難しいことが多い。**
>
> RLSを本格導入する場合はDBアクセス方式の見直しが必要：
> - Supabase client（PostgREST経由）に寄せる
> - Supabase Edge Functionsに寄せる
> - PrismaにユーザーJWTを伝播させる仕組みを入れる（複雑）
>
> **将来RLSを導入する場合は、DBアクセス方式の選定から検討すること。**
> 以下のSQL例は参考程度に留める。また実際のテーブル名はPrismaの
> マイグレーション結果（大文字小文字含む）に合わせて調整が必要。
>
> ```sql
> -- 参考例（テーブル名は実際のマイグレーション結果に合わせること）
> ALTER TABLE "Prompt" ENABLE ROW LEVEL SECURITY;
> CREATE POLICY "owner_only" ON "Prompt"
>   USING (
>     "ownerId" = (
>       SELECT id FROM "AppUser"
>       WHERE "authSubject" = auth.uid()::text
>     )
>   );
> ```

---

## gitleaks

- gitleaks設定ファイルを追加（`.gitleaks.toml`）
- pre-commitフレームワークを使い、コミット前にgitleaksが走る
- GitHub Actionsでpush/PRでもgitleaksが走る
- READMEに「導入」「実行方法」「検知されたときの対処」を記載

---

## Scripts（package.json）

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "biome:check": "biome check .",
    "biome:lint": "biome lint .",
    "biome:format": "biome format --write .",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test:e2e": "playwright test",
    "gitleaks": "gitleaks detect --source . -v"
  }
}
```

---

## 環境変数

```
# .env.local（gitignoreに含める）
DATABASE_URL=               # Supabase Postgres接続文字列
NEXT_PUBLIC_SUPABASE_URL=   # SupabaseプロジェクトURL
NEXT_PUBLIC_SUPABASE_ANON_KEY= # Supabase anon key
ALLOW_EMAILS=               # アクセスを許可するメールアドレス（カンマ区切り）例: you@example.com,test@example.com

# .env.test.local（gitignoreに含める）
TEST_USER_EMAIL=            # PlaywrightテストユーザーのEmail（ALLOW_EMAILSにも含めること）
TEST_USER_PASSWORD=         # PlaywrightテストユーザーのPassword
```

---

## README構成（Codexへの指示）

以下のセクションをREADMEに含めること：

1. **セットアップ手順**（pnpm install, prisma migrate, etc.）
2. **環境変数の設定**（上記一覧と説明）
3. **Supabase設定手順**
   - プロジェクト作成
   - GitHub OAuth設定
   - **メール/パスワード認証の有効化**（E2E用）
   - Redirect URLs設定
   - テストユーザーの作成手順
4. **Docker起動手順**（将来のローカルDB対応も含む）
5. **gitleaks運用**（導入・実行・検知時の対処）
6. **⚠️ TODOセクション（技術的負債）**
   - RLS未対応（対応手順付き）
   - GitHub OAuth E2E未対応（対応方針付き）

---

## 元仕様からの変更サマリ

| 項目 | 元仕様 | 改訂版 |
|------|--------|--------|
| Docker | 「普段はpnpm dev、必要時だけdocker」（目的曖昧） | 将来のSupabase CLIローカルDB対応前提と明記 |
| Playwright E2E | OAuthが難しければ雛形でOK | メール/パスワード認証を追加し確実にE2Eを通す |
| RLS | 「後回し」と記載のみ | 技術的負債として明示・将来の対応手順をREADMEに記載 |
| Zodバリデーション制限 | 「長さ制限」のみ（具体値なし） | 具体的な文字数・個数制限を明記 |
| UI仕様 | 未定義 | 2カラム固定・リスト型・使用コンポーネント・新規作成UX・タグ入力UIを明記 |
